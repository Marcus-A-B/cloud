* Install the express package, the nodemon tool, the mongoose driver, the body-parser (for parsing JSON objects), dotenv (for separating secrets from your source code), the joi package (for validations), bcryptjs (encryption/decryption library), and the JWT package (for generating auth-tokensd)

npm install express nodemon mongoose body-parser dotenv joi bcryptjs jsonwebtoken